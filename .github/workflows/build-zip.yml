name: Build Plugin ZIP

on:
  push:
    tags:
      - '*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to build and attach to'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}
          path: OncoCommercialGaran

      - name: Create plugin ZIP
        run: |
          rm -rf OncoCommercialGaran/.github OncoCommercialGaran/.git
          zip -r OncoCommercialGaran.zip OncoCommercialGaran

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: OncoCommercialGaran
          path: OncoCommercialGaran.zip

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
          files: OncoCommercialGaran.zip